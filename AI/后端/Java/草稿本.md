数据类型

- 基本数据类型（变量存值）

  - 字节型 byte 1字节  byte b = 10 [-128, 127]
  - 短整型 short 2字节 short s = 100 [-32768,32767]
  - 整型 int 4字节 int age = 25 [-2³¹ ~ 2³¹-1]
  - 长整型 long 8字节 long id = 100L [-2⁶³ ~ 2⁶³-1]
  - 单精度 float 4字节 float f = 3.14f  **±3.4×10³⁸**，有效数字 6~7 位
  - 双精度 double 8字节 double b = 3.14159 **±1.8×10³⁰⁸**，有效数字 15~16 位
  - 字符型 char 2字节 char c = '好' **0 ~ 65,535**（Unicode 字符）
  - 布尔型 boolean 1位 boolean b = true
  - 整数会**溢出回绕**。浮点数溢出则变成 `Infinity`

- 引用数据类型（变量存对象内存地址）

  - 数组
    - 存放一组同类型的数据
    
    - 长度固定不变，创建时确定大小
    
    - 下标从0开始，越界抛出ArrayIndexOutOfBoundsException
    
    - ```java
      // 静态初始化
      int[] arr = [1,2,3];
      // 动态初始化
      int[] arr2 = new int[5]; //默认填充0
      // 两种遍历
      for (int i = 0; i < arr.length; i++){}
      for (int num : arr) {}
      ```
    
    - 可变参数
    
      - 方法参数：类型...参数名，本质是数组，必须放在参数列表最后，类似JS中的...args
    
      - ```java
        public static int sum(int... nums){
            int total = 0;
            for (int n : nums) total += n;
            return total;
        }
        // 调用sum(1,2,3)返回
        ```
    
      - 
    
  - 字符串String

  - 类

  - 包装类

  - 集合

  - 接口

- 场景使用

  - 普通计数、循环、年龄，使用int，够用，性能好
  - 数据库主键（雪花ID） long 雪花ID约19位，超出int范围
  - 金额（分） long/BigDecimal 分用long,元用BigDecimal
  - 科学计数、物理模拟 double 
  - 文件大小、网络流量 long 可能超2GB（21亿字节）
  - 字符串字符 char

- ```java
  // 自动类型转换（小转大）
  int a = 10;
  double b = a;
  
  // 强制类型转换（大转小），可能会存在精度丢失
  double d = 3.99;
  int num = (int)d; // num = 3,小数直接截断
  ```

变量和常量

- 变量

  - Java 变量必须先声明类型再赋值

  - ```java
    int age = 18;
    ```

  - 成员变量：定义在类中，方法外；有默认初始值

  - 局部变量：定义在方法、代码块内；无默认值，使用前必须手动赋值

  - 静态变量static： 属于类，全局唯一，所有对象共享

- 常量

  - final

    - final修饰变量，只能赋值一次，不可修改。

      - ```java
        final double PI = 3.1415;
        PI = 3; //编译报错，常量不能修改
        ```

运算符

- 算术

  - ```java
    // + - * / % ++ --
    
    int 5 / 2 = 2; //整数除 舍弃小数
    ```

- 赋值

  - ```java
    // = += -= *= /=
    ```

- 比较

  - ```java
    // > < >= <= == !=
    // ==基本类型比值，引用类型比地址
    ```

- 逻辑

  - ```java
    // && || !
    ```

- 三元运算符

  - ```java
    // 条件 ? 成立值 ： 失败值
    ```

- 位运算符

  - ```java
    // & | ^ ~ << >> >>>
    ```



类

- 类与对象

  - 类：模板
  - 对象：类的实例（new 出来）
  - java创建对象必须new

- 构造方法

  - 方法名和类名完全一致，无返回值

  - 创建对象时自动调用，用于初始化成员变量

  - 一个类默认有：无参构造；若自定义有参构造后，默认无参构造消失

  - this() / super()只能写在构造第一行

  - ```java
    public class User {
        // 无参构造
        public User() {}
        // 有参构造
        public User(String name) {
            this.name = name;
        }
    }
    ```

- 方法重载Overload（同一类内）

  - 条件：方法名相同，参数列表不同（个数/类型/顺序）；和返回值、修饰符无关

- 方法重写Override（父子类之间）

  - 子类重写父类非private方法
  - 方法名、参数、返回值完全一致
  - 子类访问权限不能低于父类（父protected，子类不能private）
  - 使用@override注释校验，防止写错

- 重载和重写的区别

  - 重载：同类，同名不同参，编译期绑定
  - 重写：父子类，完全相同签名，运行期多态绑定

- OOP三大特性：封装、继承、多态

  - 封装（藏数据）

    - 核心：私有化成员变量private，对外提供get/set方法

    - 好处：控制数据合法性、隐藏内部实现、代码解耦

    - ```java
      private int age;
      public void setAge(int age) {
          if (age > 0 && age < 150) this.age = age;
      }
      ```

  - 继承extends（复用代码）

    - java单继承：一个类只能直接继承一个父亲
    - 子类拥有父类非私有属性、方法
    - 构造方法不继承，子类默认调用父类无参构造super()
    - 避免代码复用，拓展功能

  - 多态（拓展功能）

    - 核心：父类引用指向子类对象，调用重写方法时执行子类逻辑

    - 使用instanceof判断实际对象类型，强制向下转型调用子类独有方法

    - ```java
      Animal a = new Dog();
      a.eat();
      ```

- 四大访问修饰符
  - private 
    - 仅同类可访问
  - 缺省（不写）
    - 同类、同包可访问
  - protected
    - 同类、子类、同包可访问
  - public
    - 同类、子类、同包、任意包所有类可访问
- static 静态关键字
  - 修饰变量、方法、代码块、内部类
  - 静态成员属于类，全局唯一，所有对象共享
  - 调用方法：类名.静态成员，无需new 对象
  - 静态方法不能直接使用this、super、非静态变量\方法（因为静态加载早于对象）
  - 静态代码块：类加载时执行，仅运行一次，用于初始化静态资源
- final 关键字
  - final变量：常量，仅赋值一次
  - final方法：子类不能重写
  - final类：不能被继承（String 就是final类）
- this
  - 指代当前对象
  - this.属性：区分成员变量和局部变量重名
  - this(参数)：调用本类其他构造方法，必须放在构造方法第一行
- super
  - 代表父类对象
  - super.属性/方法：访问父类被重写的成员
  - super(参数)：调用父类构造方法，默认隐含，必须首行



抽象类（abstract class）

1. 

- 不能new实例；包含抽象方法（无方法体）+ 普通成员 / 方法
- 子类必须实现全部抽象方法，否则子类也声明abstract
- 使用extends只能单继承
- 有构造方法、成员变量、静态变量
- 设计思想：表明是什么，事物共性模板



接口interface（java8+）

- java8之前：仅抽象常量、抽象方法；java8新增default方法、static静态方法；java9私有方法
- 使用implements，一个类可实现多个接口，解决单继承局限
- 无构造方法：变量默认public static final常量；方法默认public abstract
- 设计思想：能做什么，行为能力规范



内部类

- 成员内部类
  - 类中定义，可直接访问外部所有成员（含private）
- 静态内部类
  - 只能访问外部静态成员，开发最常用（Builder模式）
- 局部内部类
  - 方法内定义，极少使用
- 匿名内部类
  - 无类名，快速实现接口/抽象类，Lambda替代方案



枚举（Enum）

- 本质是继承Enum的特殊类，固定有限常量场景（状态、季节、订单类型）

- 天然线程安全，可实现单例模式

- 自带values()遍历、ordinal()序号、valueof()转换

- ```java
  enum Season { SPRING,SUMMER,AUTUMN,WINTER }
  ```





包装类

- 基本类型<=>包装类

  - byte<=>Byte
  - short<=>Short
  - int<=>Integer
  - long<=>Longer
  - float<=>Float
  - double<=>Double
  - char<=>Character
  - boolean<=>Boolean

- 作用：集合、泛型只能存引用类型，必须用包装类

- 装箱/拆箱

  - 装箱：基本类型 =》 包装类 Integer i =  10;
  - 拆箱：包装类 =》 基本类型 int num = i;

- Integer缓存

  - ```java
    // Integer缓存范围：-128 ~ 127，范围内 == 为true;超出范围创建新对象， ==返回false，统一用equals()比较值。
    
    Integer a = 127;
    Integer b = 127;
    Integer c = 128;
    Integer d = 128;
    System.out.println(a==b); // true
    System.out.println(c==d); // false
    ```

    

String\StringBuilder\StringBuffer

- String

  - 底层是char数组final修饰，字符串一旦创建无法修改；拼接会产生新字符串，大量拼接性能差。

- 三者区别

  - String: 不可变，少量拼接使用
  - StringBuilder: 可变字符数组，线程不安全，单线程推荐（性能最高）
  - StringBuffer: 可变，方法加synchronized，线程安全，多线程使用

- 字符串常量池

  - 双引号字面量创建，优先存入常量池复用：new String()强制在堆创建新对象

  - ```java
    String s1 = "abc"; // 常量池
    String s2 = new String("abc"); // 堆新对象
    System.out.println(s1 == s2); //false
    ```

- 常用方法

  - 比较
    - equals() 判断内容是否相同，区分大小写，推荐用“常量.equals(变量)”写法，避免空指针
    - equalsIgnoreCase() 不区分大小写比较
    - compareTo() 比较字符串大小关系，返回负整数、零、正整数
  - 查
    - length() 返回字符串长度
    - charAt(int index) 获取制定位置的字符，索引从0开始
    - inidexOf(String str) 查找子字符串第一次出现的位置，没找到返回-1
    - contains() 判断是否包含指定内容

  - 处理（截取、转换、替换等）
    - substring(int start, int end) 截取子字符串，左闭右开区间
    - trim() 去掉字符串两端空格
    - split(String regex) 按分隔符拆分字符串返回数组
    - replace()/replaceAll() 替换字符或字符串
    - toLowerCase()/toUpperCase() 大小写转换



日期时间API

- 旧API（Date/Calender）缺陷
  - 可变对象，线程不安全
  - API混乱，月份从0开始，计算繁琐
- Java8新时间包java.time
  - LocalDate: 纯日期
  - LocalTime:纯时间
  - LocalDateTime:日期 + 时间
  - DateTimeFormatter:线程安全格式化（替代SimpleDateFormat）
  - Period/Duration:计算年月日、时分秒间隔
  - 优势：不可变对象，线程安全，语法清晰，规避旧API坑



集合（整体结构：Collection单列集合 + Map双列键值对）

- Collection

  - List有序、可重复
    - ArrayList: 底层是数组，查询快、尾部增删快，中间插入删除慢（底层数组，查询快，尾增删快，中间增删慢）
    - LinkedList: 双向链表，头尾增删快，查询慢
    - Vector：数组，全部方法加锁，线程安全，性能差，淘汰
  - Set无序、不可重复（依赖equals+hashCode去重）
    - HashSet:哈希表，无序
    - LinkedHashSet: 插入有序
    - TreeSet:自动升序排序，元素需实现Comparable

- Map（键值对，Key唯一）

  - HashMap:哈希表，无序，开发最常用
  - LinkedHashMap：插入有序
  - TreeMap:按键排序
  - Hashtable:线程安全，性能差，淘汰

  

泛型

- 作用：编译期类型校验，自动强转，消除类型转换异常

- 类型擦除：泛型仅存在编译阶段，运行时全部擦除为 Object，无泛型类型信息

- 通配符：

  - List<?> 任意类型，仅可读，不能 add（null 除外）
  - ? extends T 上界：T 及子类，只读
  - ? super T 下界：T 及父类，可写入

- ```java
  // 泛型类/泛型方法
  class Box<T>{}
  
  // 泛型方法
  public <E> void print(E e) {}
  ```





Java8新特性

- Lambda表达式‘
  - 函数式接口，仅一个抽象方法简化匿名内部类
- 方法引用::
  - 简化Lambda
- Stream API
  - 中间操作（延迟执行）
    - filter
    - map
    - flatMap
    - distinct
    - sorted
    - limit
    - skip
    - peek

  - 终端操作（触发执行）
    - collect
    - forEach
    - count
    - max/min
    - anyMach/allMach/noneMach
    - findFirst/findAny
    - reduce

  - 流只能使用一次，终端操作后流关闭，重复调用抛异常；中间操作延迟执行，等到调用终端操作才会真正计算；parellelStream并行流存在线程安全问题，不要操作外部共享变量；操作自定义对象去重、分组需要依赖equals和hashCode;尽量用Collectors收集，少用循环手动组装集合

- Optional
  - of/ofNullable/empty
  - isPresent/isEmpty
  - orElse/orElseGet/orElseThrow
  - map/flatMap/filter
  - Optional.of 和 Optional.ofNullable 区别？
    - of 包装确定不为空的值，若为空抛空指针异常
    - ofNullable 包装可能为null的值，若为null，生成一个空Optional

  - orElse 和 orElseGet 的区别？
    - orElse 不管包装值是不是空，都会创建默认对象
    - orElseGet 当包装值是空的时候才会执行生成，开销更小

  - map 和 flatMap 区别？
    - map转换函数返回普通对象
    - flatMap转换函数返回Optional，防止嵌套Optional

  - Optional 能不能解决所有空指针？
    - 只能处理引用类型判空；基础类型、数组下标越界、集合get越界无法处理；




==、equals、hashCode

- ==

  - 基本类型：比较数值
  - 引用类型：比较内存地址
  - Integer缓存，用==的话，若范围超出[-128,127]，返回为false

- equals

  - Object原生equals等价于==；String/Integer等重写后比较内容
  - 自定义类必须手动重写equals实现内容对比

- hashCode()

  - 返回对象哈希证书，用于hashSet/hashMap快速判断
  - 黄金规则：重写equals必须重写hashCode;相等对象hashCode必须相同；hash相同，对象不一定相等（哈希碰撞）

  哈希碰撞：

  - 含义：两个或多个不同的输入数据在经过哈希函数处理后产生了相同的输出值

  - 产生原因：

    - **有限的输出范围**‌：哈希函数的输出范围（即哈希表的大小）是有限的，而输入空间通常是无限的或非常大
    - **生日攻击（Birthday Attack）**：对于某些类型的哈希函数，特别是那些输出空间相对较小的，理论上存在概率使得两个输入产生相同的输出。

  - 影响：

    - **性能下降**‌：在哈希表中，如果发生碰撞，通常需要通过链表或开放寻址法等方法解决，这会增加查找、插入和删除操作的复杂度。
    - **安全风险**‌：在某些应用场景中，如密码存储或数字签名，哈希碰撞可能导致安全漏洞，例如通过找到两个不同的密码产生相同的哈希值，从而破解密码。

  - 解决策略：

    - ‌**链地址法（Chaining）**：在哈希表的每个槽位中存储一个链表，所有哈希到该槽位的元素都链接在这个链表上。
    - **开放寻址法（Open Addressing）**：当发生碰撞时，算法会寻找表中的另一个空槽位来存储这个元素。
    - **使用更强的哈希函数**：例如SHA-256等，它们提供更大的输出空间，减少碰撞的可能性。
    - **增加哈希表的大小**：通过增加哈希表的大小，可以减少碰撞的概率。
    - **双哈希法**‌：结合两个哈希函数来减少碰撞。如果第一个哈希函数发生碰撞，可以使用第二个哈希函数来确定下一个位置。

  - 例子

    ```java
    import java.util.HashMap;
    import java.util.Map;
    
    public class HashCollisionExample {
        public static void main (String[] args) {
            Map<String, String> hashMap = new HashMap<>();
            hashMap.put("key1","value1");
            hashMap.put("key2","value2"); //正常情况下不会有碰撞
            hashMap.put("anotherKey1","value1"); //这里可能会有碰撞，取决于HashMap的实现和负载因子
            
            System.out.println(hashMap.get("key1")); // 输出 value1
            System.out.println(hashMap.get("key2")); // 输出 value2
            System.out.println(hashMap.get("anotherKey1")); // 如果发生碰撞，可能输出 value1 或其他值，取决于HashMap的实现细节
        }
    }
    ```

    在这个例子中，虽然我们手动添加了两个不同的键（"key1" 和 "anotherKey1"），它们的值（"value1"）相同，理论上在标准的`HashMap`实现中可能会发生碰撞（取决于具体的实现和当前负载因子），但在大多数情况下，`HashMap`会很好地处理这种情况，通过链地址法来存储具有相同哈希值的键值对。如果你需要避免这种情况或者需要更强的碰撞抵抗能力，可以考虑使用`LinkedHashMap`或自定义更复杂的冲突解决方法。对于安全性要求高的应用，应使用如SHA-256等更安全的哈希算法。

  

