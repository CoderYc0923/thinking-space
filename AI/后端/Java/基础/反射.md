# 21. 反射

## 原理

运行时获取类的完整信息（构造器、字段、方法），动态创建对象、调用方法、修改私有属性。

## 使用场景

Spring IOC 容器、注解解析、框架动态代理

## 优缺点

优点：高度灵活，框架底层必备

缺点：性能差、破坏封装、无视 private 权限

```
Class clazz = User.class;
// 暴力访问私有字段
Field name = clazz.getDeclaredField("name");
name.setAccessible(true);
```

------