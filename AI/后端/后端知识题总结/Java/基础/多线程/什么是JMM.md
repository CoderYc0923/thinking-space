- `JMM`是**Java内存模型**，用来**规定多线程怎么读写共享变量才正确**
  - **可见性**：改动后其他线程能不能马上看见
  - **原子性**：复合操作会不会被拆开插队
  - **有序性**：指令会不会被重排导致多线程看错顺序
- **逻辑：**
  - **共享数据存储在主内存中**，每个**线程**还有自己的**工作内存**（类似CPU缓存）。线程修改后可能不立即写回，其他线程就看到旧值。
  - `volatile`,`synchronied`等就是按`JVM`规则，**控制何时写回、何时重读、何时禁止乱序**

