并发事务会引发的三个问题：

- 脏读 dirty read
  - 一个事务读到另一个事务未提交的数据，然后另一个事务失败回滚了，这条数据就是脏数据
- 不可重复读 non-repeatble read
  - 同一个事务前后读同一行数据，数据前后不一致
  - 原因：这行数据中间被其他事务提交修改了
- 幻读 phantom read
  - 同一个事务前后执行同样条件范围查询，查出来的行数不一样
  - 原因：其他事务在范围中插入删除数据

